<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gils Tech Blog</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/style.css">
</head>        

<body>
  <header class="bg-primary text-light p-4">
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container">
        <a class="navbar-brand navbar-brand-large" href="/">Gils Tech Blog</a>
        <div class="ml-auto mr-3">
          <a class="navbar-link text-light" href="/">Home</a>
          <a class="navbar-link text-light" href="/dashboard">Dashboard</a>
          {{#if logged_in}}
          <a class="navbar-link text-light" id="logout" href="/login">Logout</a>
          {{else}}
          <a class="navbar-link text-light" href="/login">Login</a>
          {{/if}}
        </div>
      </div>
    </nav>
  </header>

  <main class="container">
    {{{ body }}}
  </main>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>